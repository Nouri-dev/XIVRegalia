{% extends 'base.html.twig' %}

{% block title %}Compte de {{ app.user.firstname }} {{ app.user.lastname }} | XIV REGALIA{% endblock %}  

{% block body %}
	<main class="comptemain">
		
	  <section class="comptetitre">
			<h1>Compte de {{ app.user.firstname }} {{ app.user.lastname }}</h1> 
	  </section>

		
	  <div class="comptedivhaut">    
		<p>Nom : {{ app.user.lastname }}</p>
		<p>Prénom : {{ app.user.firstname }}</p>
	    <p>Adresse : {{ app.user.address }}</p>
		<p>Ville : {{ app.user.city }}</p>
	    <p>Code postal : {{ app.user.zipcode }}</p>
	    <p>Pays : {{ app.user.country }}</p>
	    <p>Tél : {{ app.user.phone }}</p>
	    <p>Mail : {{ app.user.email }}</p>
      </div>


	  <div class="comptefinbouton">
	        <div> 
             <a href="{{ path('profile_modifier') }}" class="bouttonblack">Modifier le profil</a>
		    </div>

			<div> 
              <a href="{{ path('profile_motdepasse')}}" class="bouttonblack">Modifier le mot de passe</a> 
			</div>
      </div> 
 
	</main>	
{% endblock %}



{# {% for message in app.flashes('message') %}
			<div class="col-xs4">
				<div class="p-3 my-2 rounded-2 success">{{message}}</div>
			</div>
		{% endfor %} #}